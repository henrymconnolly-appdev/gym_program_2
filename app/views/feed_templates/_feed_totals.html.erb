<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Totals

    <a data-toggle="collapse" href="#feed_totals_new_form">
      Add total
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @feed.totals.each do |total| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/totals/<%= total.id %>">
          <%= total.profile_id %>
        </a>

        <a href="/delete_total_from_feed/<%= total.id %>">
          Delete
        </a>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="feed_totals_new_form">
        <%= render partial: "feed_templates/feed_totals_new_form" %>
      </div>
    </div>

  </ul>
</div>
